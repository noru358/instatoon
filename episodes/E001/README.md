# E001 — 4년치 팀 단톡방을 실수로 나가버렸다

Status: **RENDER_CONTRACT_READY / NO APPROVED RASTER**

Authority:
- `STORY_SOURCE_PACK.md` — source facts, approved beats and user-gate evidence;
- `EPISODE_PLAN.json` — four-slide visual/story contract;
- `RENDER_MANIFEST.json` — required binary style media and renderer contract;
- `PRODUCTION_STATE.json` — current gate state and blockers.

Current render rule:
- S01 cannot be generated as canonical production output until the actual required D/E/sub1 binaries are supplied to an eligible renderer;
- S01 requires a physically valid phone geometry contract;
- the previously generated native-chat image is rejected and must not be reused.

Next valid production action:
1. bind the exact required reference binaries;
2. authorize asset dispatch;
3. generate S01 only;
4. present S01 to the user for the anchor gate.
