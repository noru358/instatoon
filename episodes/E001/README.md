# E001 — 4년치 팀 단톡방을 실수로 나가버렸다

Status: **S01 VISUAL PASS / S02-S04 REJECTED / CLEAN RENDER HANDOFF REQUIRED**

Authority:
- `STORY_SOURCE_PACK.md` — source facts, approved beats and user-gate evidence;
- `EPISODE_PLAN.json` — current four-slide visual/story contract;
- `RENDER_MANIFEST.json` — current binary media + renderer contract bound to the plan;
- `PRODUCTION_STATE.json` — current gate state, rejection evidence and blocker.

## Preserved approval

S01:
- user-approved visual/taste anchor;
- do not regenerate unless the user explicitly withdraws that approval;
- not yet a machine-authoritative raster because no repository-bound artifact hash/attempt record exists;
- the exact approved S01 image must be re-supplied in the clean render context for S02+ continuity.

## Rejected outputs

All S02-S04 candidates produced after the old conversation render context became unsafe are quarantined.

Latest set-level QC found:
- repeated safe portrait / directional-composition bias;
- insufficient S02→S03 visual delta;
- S03 archive-loss information too generic/abstract;
- S04 right-side shoulder/torso/arm anatomy failure;
- S04 semantic acting mismatch: too bright/cute instead of hollow resigned crying-laughter.

They are not episode assets and must not be used as anchors or repair sources.

## Next valid production action

1. move to a clean dedicated render context;
2. restore Git authority and validate the active E001 package;
3. supply D/E/sub1 plus the exact approved S01 image as actual media;
4. resume from **S02**;
5. render S02 → S03 → S04 sequentially, one image per slide, text-free, with internal QC after each;
6. run complete-set sequence QC;
7. return to the user only at the complete raster-set gate.
